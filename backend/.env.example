PORT=4000
# Same project as the dashboard URL (AAURA: njmsxkatexarayrvzcsq)
SUPABASE_URL=https://njmsxkatexarayrvzcsq.supabase.co
# Publishable/anon key from Project Settings → API (public client / anon)
SUPABASE_ANON_KEY=your-anon-or-publishable-key
# service_role key — backend only; keep secret and out of git
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
JWT_AUDIENCE=authenticated
AI_SERVICE_URL=http://localhost:8000
# Production: comma-separated web app origins (Firebase / Netlify URL)
# CORS_ORIGINS=https://your-project.web.app

# One-off scripts only (set-password.ts) — never commit real values
# SET_PASSWORD_USER_ID=
# SET_PASSWORD_VALUE=
